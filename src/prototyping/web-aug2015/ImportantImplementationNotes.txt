Important Implementation Notes
===


Things to Cleanup in JavaScript:

-	unbind event listeners on objects prior to deletion
-	be careful about clearing timeouts and intervals 
-	ensure consistency of argument types to functions and within objects
-	avoid mixing types in arrays
-	avoid removing elements from arrays!
-	profile!
-	consider using DocumentFragment for card layout construction code and UI widgets to improve performance
	(processing off DOM)
-	avoid touching the DOM & causing reflow
